# Combined dependencies for the local HMI stack.
# Install this in the repository root venv used by systemd:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements-hmi.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0

python-can>=4.0.0
cantools>=39.0.0
pyzmq>=25.0.0
requests>=2.28.0
numpy
